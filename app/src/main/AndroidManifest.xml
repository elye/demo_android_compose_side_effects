<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.composesideeffects">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeSideEffects">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeSideEffects.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LaunchedEffectActivity" />
        <activity android:name=".SideEffectActivity" />
        <activity android:name=".ProduceStateActivity" />
        <activity android:name=".ControllableLaunchedEffectActivity" />
        <activity android:name=".RememberCoroutineScopeActivity" />
        <activity android:name=".CancelDetectLaunchedEffectActivity" />
        <activity android:name=".RememberUpdateStateActivity" />
        <activity android:name=".DisposableEffectActivity" />
        <activity android:name=".DerivedStateOfActivity" />
        <activity android:name=".SnapshotFlowActivity" />
    </application>

</manifest>